# Workflow Guide

## 1. Overview

This document defines the workflow specification for the Geelato platform.

## 2. File Structure

workflow/
`└── {workflowName}/`
    `└── {workflowName}.workflow.json`  # Workflow definition

## 3. BPMN 2.0 Specification

Workflows use standard BPMN 2.0 XML format.
